<ul class="nav navbar-nav navbar-right">
    @if (Request.IsAuthenticated)
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">@ViewBag.UserName <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                <li>@Html.ActionLink("Modifier mon profil", "Edit", "User")</li>
                <li>@Html.ActionLink("Changer mon mot de passe", "ChangePassword", "Account")</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Déconnexion", "LogOff", "Account")</li>
            </ul>
        </li>
    }
    else
    {
        <li>@Html.ActionLink("S'inscrire", "Register", "Account")</li>
        <li>@Html.ActionLink("Se connecter", "LogOn", "Account")</li>
    }
</ul>
