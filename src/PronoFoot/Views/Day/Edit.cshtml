@model PronoFoot.ViewModels.DayEditViewModel

@{
    ViewBag.Title = "Modifier";
}

<h2>Modifier @Model.DayName</h2>

@Html.Partial("_DayForm", Model.Day)

<div>
    @Html.ActionLink("Annuler", "Details", new { id = Model.Day.DayId })
</div>
